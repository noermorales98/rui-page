# CLAUDE.md

Apunta a `AGENTS.md` y a `skills/`. Mismas reglas. Este archivo existe para que Claude Code lo lea automáticamente al iniciar sesión en el repo.

## Lectura inicial obligatoria
1. `AGENTS.md`
2. `skills/sdd-loader.md` → ejecuta su procedimiento.
3. La skill del módulo que vas a tocar (`skills/module-*.md`).

## Atajos
- Quiero crear/editar schema → `skills/prisma-migration.md` + `docs/sdd/DATA_MODEL.md`.
- Quiero agregar server action → `skills/zod-validator.md` + `skills/role-guard.md` + `skills/audit-logger.md`.
- Quiero revisar trabajo de otro agente → `skills/reviewer-agent.md`.
- Quiero escribir tests → `skills/test-writer.md`.

## Recordatorio
El SDD (`docs/sdd/`) es la fuente de verdad. Cualquier discrepancia con el código se resuelve actualizando uno de los dos. No improvises.
