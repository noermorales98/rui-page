import { TOK } from '@/app/crm/_lib/ui-tokens'

export default function CursosPage() {
  return (
    <div className="flex flex-col gap-6">
      <div className={`${TOK.panel} p-12 text-center`}>
        <p className={TOK.textMuted}>Módulo en construcción</p>
      </div>
    </div>
  )
}
